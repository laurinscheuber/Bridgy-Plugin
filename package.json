{
  "name": "awall-synch",
  "version": "1.0.0",
  "description": "Figma plugin for synchronizing design tokens and components",
  "main": "code.js",
  "scripts": {
    "build": "npm run bundle && node build.js",
    "bundle": "esbuild src/index.js --bundle --platform=browser --outfile=code.js",
    "dev": "esbuild src/index.js --bundle --platform=browser --watch --outfile=code.js"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@figma/plugin-typings": "^1.82.0",
    "esbuild": "^0.25.5"
  }
}
